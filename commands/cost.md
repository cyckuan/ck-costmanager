---
description: Track and report token usage costs
allowed-tools: ["Bash"]
argument-hint: "<report|projects|budget <USD>|off|on|reset>"
---

Run this command exactly and display the raw output with no additional commentary:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/cost.js" $ARGUMENTS
```
